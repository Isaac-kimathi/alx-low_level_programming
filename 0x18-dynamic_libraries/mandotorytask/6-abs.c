#include "main.h"
/**
 * _abs - function that computes the absolute value of an integer
 *
 * @d: input no
 *
 * Return: absolute
 */
int _abs(int d)
{
	if (d >= 0)
	{
		return (d);
	}
	else
	{
		return (d * -1);
	}
}
