#include "main.h"
#include <string.h>
/**
 * _strcmp - function that compares two strings
 * @s1: string pointer 1
 * @s2: string pointer 2
 * Return: int
 */
int _strcmp(char *s1, char *s2)
{
	int q;

	q = strcmp(s1, s2);
	return (q);
}
